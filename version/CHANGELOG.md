# V1.7.8

- 修正 `views.js` 使用 `APP_VERSION_LABEL` / `SCHEMA_VERSION` 但沒有 import，造成總覽頁啟動 ReferenceError。
- 統一 `version-info.js` 快取參數到 V1.7.8。
- 修正 `simulator3d.js` 仍載入舊版 version-info 快取參數。
- 首頁、Runtime、Debug、Google Cloud、Apps Script 與 index 快取同步為 V1.7.8。
