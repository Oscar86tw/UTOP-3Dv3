import {featureGroups,scenes,events,devices} from './data.js';
import {state} from './state.js';
const esc=s=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const rows=items=>`<div class="list">${items.map(i=>`<div class="list-row"><div class="grow"><b>${esc(i[0])}</b><div class="muted">${esc(i[1])}</div></div>${i[2]||''}</div>`).join('')}</div>`;
export function renderOverview(){return `
<div class="grid g2"><div class="metric"><span class="muted">系統群</span><strong>6</strong></div><div class="metric"><span class="muted">主要工作區</span><strong>2D + 3D</strong></div><div class="metric"><span class="muted">裝置</span><strong>手機 / 平板 / 電腦</strong></div><div class="metric"><span class="muted">定位</span><strong>工程數位孿生</strong></div></div>
<div class="card"><div class="section-title"><h2>UTOP-3Dv3 整體架構</h2><span class="badge">Prototype</span></div>${rows(featureGroups)}</div>
<div class="callout"><b>核心原則：</b>同一份專案資料同時驅動 2D、3D、設備、接線、車輛、任務、Signal Trace、簡報與多螢幕。</div>`}
export function renderSimulator(){return `
<div class="card"><div class="section-title"><h2>3D 車道互動模擬</h2><span class="badge" id="simStatus">待命</span></div>
<div class="sim-stage" id="simStage"><div class="guard"></div><div class="road"></div><div class="loop-zone ${state.simulator.loop?'on':''}" id="loopZone"></div><div class="signal-line ${state.simulator.signals?'on':''}" id="signalLine"></div><div class="barrier-box"></div><div class="barrier-arm ${state.simulator.barrier?'open':''}" id="barrierArm"></div><div class="car" id="car"></div></div>
<div class="drive-pad"><span class="blank"></span><button data-drive="forward">▲ 前進</button><span class="blank"></span><button data-drive="left">◀ 左</button><button data-drive="stop">■ 停止</button><button data-drive="right">右 ▶</button><span class="blank"></span><button data-drive="back">▼ 後退</button><span class="blank"></span></div>
<div class="toolbar engineering-only"><button id="toggleSignals">DI/DO線</button><button id="resetCar">重設車輛</button><button id="saveView">＋記錄視野</button></div>
<div class="hint">電腦版可用 W/A/S/D；手機使用按鈕。車輛進入地感區會觸發 Loop → DI1 → Relay → DO1 → Barrier。</div></div>`}
export function renderScene(){return `
<div class="card"><h2>情境場景套用</h2><div class="form-grid"><div class="field"><label>地點</label><select id="scenePlace">${scenes.map(x=>`<option ${x===state.scene.place?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>時間</label><select id="sceneTime">${['白天','黃昏','夜間','凌晨'].map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field"><label>天候</label><select id="sceneWeather">${['晴天','小雨','大雨','霧'].map(x=>`<option>${x}</option>`).join('')}</select></div><div class="field"><label>事件</label><select id="sceneEvent">${events.map(x=>`<option>${x}</option>`).join('')}</select></div></div><button id="applyScene" class="primary-btn" style="width:100%;margin-top:10px">套用情境</button><div id="sceneSummary" class="callout" style="margin-top:10px">目前：${state.scene.place} · ${state.scene.time} · ${state.scene.weather} · ${state.scene.event}</div></div>
<div class="card"><div class="section-title"><h2>Saved Viewpoints</h2><button id="addView">＋儲存視野</button></div>${rows(state.viewpoints.map((v,i)=>[v,`Saved View ${i+1}`,`<button data-view="${i}">前往</button>`]))}</div>`}
export function renderHotkeys(){return `<div class="card"><div class="section-title"><h2>快捷鍵管理</h2><span class="badge">Web-safe</span></div><div class="field"><label>功能</label><select id="hotkeyAction"><option>入口柵欄機 OPEN</option><option>入口柵欄機 CLOSE</option><option>地感01 ON/OFF</option><option>ETAG讀頭01 TRIGGER</option><option>下一個視野</option></select></div><button id="captureHotkey" style="width:100%;margin-top:10px">直接按下要設定的鍵</button><div id="captureStatus" class="callout" style="margin-top:10px">尚未進入按鍵捕捉模式。</div></div><div class="card"><h2>Hotkey List</h2>${rows(state.hotkeys.map(h=>[h.key,`${h.target} · ${h.action}`,`<span class="badge">啟用</span>`]))}</div>`}
export function renderDisplay(){return `<div class="card"><div class="section-title"><h2>多螢幕同步</h2><button id="addDisplay">＋加入裝置</button></div>${rows(state.displays.map(d=>[d.name,`${d.mode} · ${d.resolution}`,`<span class="badge ${d.state==='ONLINE'?'status-ok':''}">${d.state}</span>`]))}</div><div class="card"><h3>支援方向</h3><div class="grid g2"><div class="metric">Browser Display</div><div class="metric">Cast / AirPlay</div><div class="metric">HDMI</div><div class="metric">Offline / LAN</div></div><p class="muted">顯示端可只呈現 3D、任務提示、DI/DO、標註，不顯示工程控制介面。</p></div>`}
export function renderMission(){const steps=['ETAG偵測','Controller DI1 ON','Relay NO CLOSED','DO1 ON','Barrier OPEN'];return `<div class="card"><div class="section-title"><h2>Mission / Dependency</h2><button id="playMission" class="primary-btn">▶ 播放</button></div><div class="timeline" id="missionTimeline">${steps.map((s,i)=>`<div class="step" data-step="${i}"><div class="dot">${i+1}</div><div><b>${s}</b><div class="muted">${i===0?'等待車輛進入':'由上一節點解鎖'}</div></div></div>`).join('')}</div><div id="missionState" class="callout" style="margin-top:10px">任務待命。</div></div>`}
export function renderEngineering(){return `<div class="card"><h2>設備與工程</h2>${rows(devices.map(d=>[`${d.id} ${d.name}`,`${d.type} · ${d.floor}`,`<span class="badge">${d.state}</span>`]))}</div><div class="card"><h3>接線驗證</h3><div class="form-grid"><div class="field"><label>來源</label><select id="wireFrom"><option>Controller DO1</option><option>Controller +12V</option><option>Controller GND</option></select></div><div class="field"><label>目標</label><select id="wireTo"><option>Barrier OPEN</option><option>Barrier +24V</option><option>Barrier GND</option></select></div></div><button id="validateWire" style="width:100%;margin-top:10px">檢查接線</button><div id="wireResult" class="callout" style="margin-top:10px">尚未檢查。</div></div><div class="card"><h3>Cable / BOM</h3><div class="grid g3"><div class="metric"><span class="muted">Cat6</span><strong>128m</strong></div><div class="metric"><span class="muted">2C控制線</span><strong>86m</strong></div><div class="metric"><span class="muted">預留</span><strong>10%</strong></div></div></div>`}
export function renderNetwork(){return `<div class="grid g2"><div class="metric"><span class="muted">PoE 預算</span><strong>240W</strong></div><div class="metric"><span class="muted">目前使用</span><strong>126W</strong></div><div class="metric"><span class="muted">網路流量</span><strong>184 Mbps</strong></div><div class="metric"><span class="muted">錄影需求</span><strong>18.4 TB</strong></div></div><div class="card"><h2>Network Topology</h2>${rows([['Camera01 + Camera02','PoE → Switch01'],['Switch01','1GbE → Core Switch'],['Core Switch','→ NVR01'],['UPS','AC110V → DC / Network Backup']])}</div>`}
export function renderField(){return `<div class="card"><h2>現場工具</h2>${rows([['施工前後比較','透明疊圖、左右分割、差異顯示'],['Photo Pin','照片綁定3D位置與設備'],['Commissioning','ETAG → DI → Relay → DO → Barrier PASS/FAIL'],['Issue / Punch List','3D位置建立待修問題'],['QR設備標籤','掃碼開設備3D位置、端子、照片、維修紀錄'],['Replay','事件時間軸 0.25x / 0.5x / 1x / 2x'],['手機簡報遙控器','視野、任務、柵欄、DI/DO 遠端操作']])}</div>`}
export function renderProject(){return `<div class="grid g2"><div class="metric"><span class="muted">Auto Save</span><strong>ON</strong></div><div class="metric"><span class="muted">Schema</span><strong>v3.0</strong></div><div class="metric"><span class="muted">Undo</span><strong>12</strong></div><div class="metric"><span class="muted">Redo</span><strong>3</strong></div></div><div class="card"><div class="section-title"><h2>Project Snapshot</h2><button id="addSnapshot">建立快照</button></div>${rows(state.snapshots.map((s,i)=>[s,`Snapshot ${i+1}`,`<button>還原</button>`]))}</div><div class="card"><h2>Debug Center</h2>${rows([['FPS','58'],['設備數','24'],['連線數','31'],['錯誤','0'],['2D/3D Sync','正常'],['Multi Display','正常'],['單一資料夾檔案上限','90']])}</div>`}
export function render(route){return ({overview:renderOverview,simulator:renderSimulator,scene:renderScene,hotkeys:renderHotkeys,display:renderDisplay,mission:renderMission,engineering:renderEngineering,network:renderNetwork,field:renderField,project:renderProject}[route]||renderOverview)()}
