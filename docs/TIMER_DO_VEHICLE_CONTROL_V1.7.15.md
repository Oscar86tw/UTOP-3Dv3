# V1.7.15 車輛控制與 Timer DO 輸出

## 車種切換
在 3D 工作區下方車輛控制區直接按「🚗 汽車」或「🛵 機車」。切換會更新 3D 模型、HUD 與地感車體尺寸判定。

## 倒數計時器訊號流程
DI1 開始倒數 → RUNNING → 0 秒 → DONE → DO1 ON 0.45 秒 → Connection → 目標 DI ON → 回 OFF。

即使 DO1 沒有接線，Timer 自己的接線卡仍會顯示 DO1 ON，並在事件紀錄寫入倒數完成輸出。
